---
- name: Restart apps
  ansible.builtin.command:
    cmd: docker-compose restart
    chdir: "/opt/apps/{{ item }}"
  loop:
    - crapi
    - juiceshop
    - vampi
    - dvga
    - pixi
  changed_when: true
