plugin: amazon.aws.aws_ec2
regions:
  - us-east-2

filters:
  tag:Project: akamai-lab
  tag:Environment: lab
  instance-state-name: running

keyed_groups:
  - key: tags.Role
    prefix: role
    separator: "_"
  - key: tags.Name
    prefix: name
    separator: "_"

hostnames:
  - private-ip-address

compose:
  ansible_host: private_ip_address
